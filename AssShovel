--https://docs.sirius.menu/rayfield
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "whatever this thing is i forgot too",
   Icon = 0, -- Use 0 for no icon
   LoadingTitle = "hi there bitch",
   LoadingSubtitle = "made for niggaring kecil man",
   ShowText = "hi there nig", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Amber",

   ToggleUIKeybind = "L",

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },

   KeySystem = true,
   KeySettings = {
      Title = "if you read this abi likes you",
      Subtitle = "if you read this abi likes you",
      Note = "haha nigger i put key so you must open the raw link",
      FileName = "Idk",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"battleunderabrokensky-----67"}
   }
})

local Tab = Window:CreateTab("idk bro i forgot", 4483362458) -- Title, Image

local Button = Tab:CreateButton({
   Name = "Swing Duration",
   Callback = function()
   local TARGET_NAME = "swingduration"
local NEW_VALUE = 0.3

print("----- Setting all '" .. TARGET_NAME .. "' values to " .. NEW_VALUE .. " -----")

local changed = 0

for _, obj in ipairs(game:GetDescendants()) do
    if obj.Name == TARGET_NAME and obj:IsA("NumberValue") then
        print("Changed:", obj:GetFullName(), " | Old Value:", obj.Value)
        obj.Value = NEW_VALUE
        changed += 1
    end
end

print("Done. Modified", changed, "NumberValue(s).")
   end,
})

local Button = Tab:CreateButton({
   Name = "Cooldown",
   Callback = function()
   local TARGET_NAME = "cooldown"
local NEW_VALUE = 0.3

print("----- Setting all '" .. TARGET_NAME .. "' values to " .. NEW_VALUE .. " -----")

local changed = 0

for _, obj in ipairs(game:GetDescendants()) do
    if obj.Name == TARGET_NAME and obj:IsA("NumberValue") then
        print("Changed:", obj:GetFullName(), " | Old Value:", obj.Value)
        obj.Value = NEW_VALUE
        changed += 1
    end
end

print("Done. Modified", changed, "NumberValue(s).")
   end,
})

local Button = Tab:CreateButton({
   Name = "Charge Cooldown",
   Callback = function()
   local TARGET_NAME = "chargetime"
local NEW_VALUE = 0.1

print("----- Setting all '" .. TARGET_NAME .. "' values to " .. NEW_VALUE .. " -----")

local changed = 0

for _, obj in ipairs(game:GetDescendants()) do
    if obj.Name == TARGET_NAME and obj:IsA("NumberValue") then
        print("Changed:", obj:GetFullName(), " | Old Value:", obj.Value)
        obj.Value = NEW_VALUE
        changed += 1
    end
end

print("Done. Modified", changed, "NumberValue(s).")
   end,
})

local Button = Tab:CreateButton({
   Name = "Weight",
   Callback = function()
   local TARGET_NAME = "weight"
local NEW_VALUE = 0.1

print("----- Setting all '" .. TARGET_NAME .. "' values to " .. NEW_VALUE .. " -----")

local changed = 0

for _, obj in ipairs(game:GetDescendants()) do
    if obj.Name == TARGET_NAME and obj:IsA("NumberValue") then
        print("Changed:", obj:GetFullName(), " | Old Value:", obj.Value)
        obj.Value = NEW_VALUE
        changed += 1
    end
end

print("Done. Modified", changed, "NumberValue(s).")
   end,
})
